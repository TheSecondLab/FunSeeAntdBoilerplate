const baseConfig = require('./config.base');

// const ERROR_LOG = `${logPath}/flying-phoenix-web_error.log`;
// const RESP_LOG = `${logPath}/flying-phoenix-web_all.log`;

export default {
  ...baseConfig
};
